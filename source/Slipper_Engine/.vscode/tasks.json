{
    "tasks": [
        {
            "type": "shell",
            "label": "Build Debug",
            "command": "cmake --build ${workspaceRoot}/build --config Debug",
            "options": {
                "cwd": "${workspaceRoot}/build"
            },
        },
        {
            "type": "shell",
            "label": "Build Release",
            "command": "cmake --build ${workspaceRoot}/build --config Release",
            "options": {
                "cwd": "${workspaceRoot}/build"
            },
        }
    ],
    "version": "2.0.0"
}