{
    "tasks": [
        {
            "type": "shell",
            "label": "Build Debug",
            "command": "cmake --build ${workspaceRoot}/build --config Debug --target all -j 18 --",
            "options": {
                "cwd": "${workspaceRoot}"
            },
        },
        {
            "type": "shell",
            "label": "Build Release",
            "command": "cmake --build ${workspaceRoot}/build --config Release --target all -j 18 --",
            "options": {
                "cwd": "${workspaceRoot}"
            },
        }
    ],
    "version": "2.0.0"
}